#doctrine:
#    dbal:
#        # Overrides the sqlite name in the test environment only
#        # "host", "port", "username", & "password" can also be set to override their respective url parts
#        #
#        # If you're using ParaTest, "TEST_TOKEN" is set by ParaTest otherwise nothing is appended to the sqlite name.
#        dbname: main_test%env(default::TEST_TOKEN)%
